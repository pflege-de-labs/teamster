// Package store holds the Store interface and the backends behind it.
//
// The query code in sqlitedb is generated by sqlc from the .sql files in
// queries/, type-checked against the goose migrations in migrations/, and
// committed — so building the service needs no generator, the same bargain the
// admin UI's templ output makes.
package store

//go:generate ../../bin/sqlc generate --file sqlc.yaml
